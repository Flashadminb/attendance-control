# ── ข้อมูลพนักงานจริง — ห้ามขึ้น repo ───────────────────────────────────
# ไฟล์ที่ดึงจาก HCM มีชื่อ-สกุล รหัสพนักงาน และประวัติการลา (รวมลาป่วย/ลาคลอด)
# ของพนักงานจริง เป็นข้อมูลส่วนบุคคล เก็บไว้ในเครื่องเท่านั้น
uploads/
*.xlsx
*.xls
*.csv

# ไฟล์ที่ export ออกจากแอป
Attendance*.xml
*.png

# ระบบ
.DS_Store
Thumbs.db
desktop.ini
.claude/
